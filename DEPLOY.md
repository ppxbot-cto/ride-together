# 一起骑 RT - 部署指南 🚀

> 从代码到上线的完整流程

---

## 📋 部署前检查清单

### ✅ 已完成
- [x] 注册微信小程序账号
- [x] 小程序名称：**一起骑 RT**
- [x] AppID：`wxebc7acc8fde8d204`
- [x] 类目：**工具 > 信息查询**
- [x] 代码已推送到 GitHub

### ⏳ 待完成
- [ ] 下载微信开发者工具
- [ ] 开通云开发服务
- [ ] 创建数据库集合
- [ ] 上传云函数
- [ ] 编译预览
- [ ] 提交审核

---

## 🛠️ 第一步：下载微信开发者工具

### 下载地址
https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html

### 版本选择
- **稳定版（Stable Build）** ← 推荐
- 操作系统：根据你的电脑选择（Windows / macOS）

### 安装后
1. 打开微信开发者工具
2. 使用微信扫码登录
3. 选择「小程序项目」

---

## 📦 第二步：导入项目

### 方式 A：从 GitHub 克隆
```bash
# 1. 克隆项目
git clone https://github.com/ppxbot-cto/ride-together.git
cd ride-together

# 2. 用微信开发者工具打开
# 选择「项目」→ 导入项目 → 选择本地项目目录
```

### 方式 B：直接下载 ZIP
1. 访问：https://github.com/ppxbot-cto/ride-together
2. 点击「Code」→ 「Download ZIP」
3. 解压到本地
4. 用微信开发者工具打开

---

## ☁️ 第三步：开通云开发

### 开通步骤
1. 在微信开发者工具顶部工具栏，点击「云开发」按钮
2. 首次使用会提示开通，点击「开通」
3. 选择「免费版」即可（初期够用）
4. 创建环境名称：`ride-together`
5. 记录下**环境 ID**（类似：cloud1-xxx）

### 配置云开发环境
编辑 `miniprogram/app.js`，找到顶部的 `CLOUD_ENV_ID`：
```javascript
const CLOUD_ENV_ID = '';  // 与微信开发者工具里 cloudfunctions 所选环境一致
```
- **若登录一直失败**：把云开发控制台里的「环境 ID」复制过来，填进引号内（如 `'cloud1-xxxx'`）。
- 留空则使用默认环境（第一个创建的环境）。

---

## 🗄️ 第四步：创建数据库集合

### 在云开发控制台操作

1. 点击「云开发」→ 「数据库」
2. 点击「+」添加集合

### 创建以下集合

#### 1. activities 集合
- 集合名称：`activities`
- 权限设置：**所有用户可读写**

#### 2. users 集合
- 集合名称：`users`
- 权限设置：**所有用户可读写**

### 权限配置示例
```json
{
  "read": "auth.openid != null",
  "write": "auth.openid != null"
}
```

---

## ☁️ 第五步：上传云函数

### ⚠️ 必须先选择云环境（避免报错）

若出现错误：**「请在编辑器云函数根目录（cloudfunctionRoot）选择一个云环境」**，按下面操作：

1. 在微信开发者工具左侧文件树，找到 **`cloudfunctions`** 文件夹（云函数根目录）
2. **右键点击** `cloudfunctions` 文件夹
3. 在菜单中选择「**当前环境**」或「**选择云环境**」
4. 在列表里选择你已开通的云环境（如 `ride-together` 或你的环境 ID）
5. 选好后，`cloudfunctions` 旁会显示当前环境名称，再进行上传

### 上传步骤
1. 在微信开发者工具左侧，找到 `cloudfunctions` 目录
2. **右键点击** `cloudfunctions` 文件夹
3. 选择「**上传并部署：云端安装依赖**」
4. 等待上传完成（可能需要几分钟）

### 需要上传的云函数
```
cloudfunctions/
├── login/                ✅
├── updateUserProfile/    ✅ 更新用户头像、昵称（「我的」页使用）
├── createActivity/       ✅
├── getActivities/        ✅
├── joinActivity/         ✅
├── cancelJoin/           ✅
└── getMyActivities/      ✅
```

### 验证上传成功
- 云开发控制台 → 云函数
- 应该能看到 6 个函数都已部署

---

## ▶️ 第六步：编译预览

### 模拟器预览
1. 点击工具栏「编译」按钮
2. 在模拟器中查看小程序
3. 测试各个功能：
   - 首页活动列表
   - 发布活动
   - 活动详情
   - 加入/取消

### 真机预览
1. 点击工具栏「预览」按钮
2. 使用微信扫码
3. 在手机上测试

### 测试清单
- [ ] 微信登录是否正常
- [ ] 发布活动是否成功
- [ ] 活动列表是否显示
- [ ] 加入活动是否成功
- [ ] 取消报名是否成功
- [ ] 我的活动页面是否正常

---

## 📤 第七步：上传代码

### 上传步骤
1. 点击右上角「上传」按钮
2. 填写版本信息：
   - **版本号**：`1.0.0`
   - **项目备注**：`Beta 版本 - 基础功能上线`
3. 点击「上传」

### 版本记录
```
1.0.0 - Beta 版本
- 活动发布功能
- 活动浏览功能
- 加入/取消功能
- 个人中心
```

---

## 📝 第八步：提交审核

### 在小程序后台操作

1. 访问：https://mp.weixin.qq.com/
2. 扫码登录
3. 左侧菜单：版本管理
4. 找到刚上传的「开发版本」
5. 点击「提交审核」

### 审核所需材料

#### 1. 功能页面截图
- 首页截图
- 发布页截图
- 详情页截图
- 个人中心截图

#### 2. 用户协议
（可以参考以下模板）

#### 3. 隐私政策
（可以参考以下模板）

#### 4. 测试账号
- 如果小程序需要登录，提供测试账号
- 一起骑使用微信登录，无需额外账号

---

## ⚠️ 审核注意事项

### 类目选择建议

如果还没确定类目，建议：

| 优先级 | 类目 | 说明 |
|--------|------|------|
| ⭐⭐⭐ | 体育 - 运动 | 最符合骑行主题 |
| ⭐⭐ | 工具 - 信息 | 审核较快 |
| ⭐ | 社交 - 交友 | 可能需要额外资质 |

### 避免的关键词

在小程序名称、介绍、页面中避免：
- ❌ "组织"、"官方"、"第一"
- ❌ "最"、"全国"、"全球"
- ✅ 用"分享"、"交流"、"平台"

### 必备内容

1. **用户协议** - 说明服务条款
2. **隐私政策** - 说明数据如何使用
3. **联系方式** - 邮箱或客服电话

---

## 📄 用户协议模板

```markdown
# 一起骑用户协议

## 1. 服务说明
一起骑是一个骑行活动分享平台，用户可以发布和参与骑行活动。

## 2. 用户责任
- 用户发布的内容应真实、合法
- 活动风险由参与者自行承担
- 遵守法律法规，不发布违规内容

## 3. 免责声明
- 平台仅提供信息展示，不参与活动组织
- 活动安全由组织者和参与者自行负责
- 不可抗力导致的损失，平台不承担责任

## 4. 联系方式
如有问题，请联系：[你的邮箱]

## 5. 协议更新
平台有权更新本协议，更新后会公示。
```

---

## 📄 隐私政策模板

```markdown
# 一起骑隐私政策

## 1. 信息收集
我们收集以下信息：
- 微信昵称、头像（用于展示）
-  openid（用于识别用户）

## 2. 信息使用
收集的信息用于：
- 显示用户信息
- 关联用户发布的活动

## 3. 信息保护
- 不会向第三方泄露用户信息
- 不会用于商业推广

## 4. 用户权利
- 用户可以查看自己的信息
- 用户可以删除自己发布的内容

## 5. 政策更新
我们会适时更新本政策，重大变更会通知用户。
```

---

## 🔧 常见问题

### Q1: 一直显示「登录失败」
**A:** 按顺序检查：
1. **云环境一致**：`miniprogram/app.js` 里 `CLOUD_ENV_ID` 须与开发者工具中「右键 cloudfunctions → 当前环境」一致；或先改为 `''` 使用默认环境试一下。
2. **云函数已部署**：确认已上传并部署 `login` 云函数（云开发控制台 → 云函数 中能看到）。
3. **数据库存在**：云开发控制台 → 数据库 中要有 `users` 集合。
4. 看控制台报错：若提示与 env / environment 相关，多半是环境 ID 不一致。

### Q2: 云函数上传失败
**A:** 检查网络连接，或尝试：
```bash
cd cloudfunctions/login
npm install
# 然后重新上传
```

### Q3: 数据库权限错误
**A:** 在云开发控制台检查集合权限，确保设置为「所有用户可读写」

### Q4: 获取用户信息失败
**A:** 微信要求用户主动授权，确保使用 `wx.getUserProfile` API

### Q5: 审核被拒绝
**A:** 根据拒绝原因修改，常见问题：
- 类目不符 → 调整类目
- 缺少资质 → 补充材料
- 内容违规 → 修改文案

---

## 📞 技术支持

- GitHub Issues: https://github.com/ppxbot-cto/ride-together/issues
- 开发者文档：https://developers.weixin.qq.com/miniprogram/dev/framework/

---

## ✅ 上线后检查

上线后记得：
- [ ] 分享给朋友测试
- [ ] 监控云函数使用情况（免费额度）
- [ ] 收集用户反馈
- [ ] 准备 V1.1 版本功能

---

_祝上线顺利！_ 🎉
